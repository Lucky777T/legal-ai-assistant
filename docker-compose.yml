 
version: '3.8'

services:
  endee:
    image: endeeio/endee-server:latest
    container_name: endee-legal
    ports:
      - "8080:8080"
    environment:
      NDD_DATA_DIR: /data
      NDD_NUM_THREADS: 0
    volumes:
      - endee_data:/data
    restart: unless-stopped

volumes:
  endee_data: